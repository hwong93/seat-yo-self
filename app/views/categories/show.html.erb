<h1> <%= @category.name %> Restaurants </h1>

<% @category.restaurants.each do |rest| %>
<p>
  <%= link_to rest.name, restaurant_path(rest.id) %> | <% rest.price.times do %>
    <%= p '$' %>
  <% end %>
</p>

<% end %>
